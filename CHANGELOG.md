# 公开更新日志

面向老师、部署者和公开读者。仅保留功能、体验、部署和通用安全加固信息，不记录敏感漏洞细节、内部调试入口或开发环境流水。

## 2026-06-05

### 2.3.0
- `main` 分支重置为首次公开发布，单 commit 干净历史；`preview/dev` 保留完整开发记录。
- 版本号升至 2.3.0，标志项目进入稳定公开发布阶段。
- 明确版本与日志策略：`preview/dev` 承接日常开发和预览期细日志，`main` 只承接收敛后的正式版本发布。
- 明确正式版本推进方式：小批量稳定功能优先走 minor，重大结构调整才升 major，正式版紧急修复可走 patch。
- 公开协作文档同步补充 AI 汇报要求和发布约定，减少预览日志与正式发布说明混写造成的噪音。

## 2026-06-05

### 2.2.32
- 清理公开仓库中的历史单页构建产物，不再保留 `dist/` 下导出的正式版 HTML。
- 补充 `.gitignore`，继续忽略本地 AI 协作目录和临时草稿文件，减少工作区被本地文件污染。
- 同步 `打包部署说明.md`，改为当前真实的启动、默认管理员和 PyInstaller 打包方式说明。

## 2026-06-05

### 2.2.31
- 合并到公开分支前补做一轮文件审核：公开 README 不再引用本地专用协作文件，避免把本地 AI 协作入口误当成公开文档入口。
- `start.bat` 不再在启动时自动切换 Git 分支，减少对公开仓库工作区的意外改动。

## 2026-06-04

### 2.2.30
- 重写 `README.md` 和 `README.zh-CN.md`，统一为当前项目真实能力、启动方式、文档地图和公开仓库边界的双语说明。

## 2026-06-04

### 2.2.29
- 新增 PVP 期间退出限制：房间进入倒计时或开战后不能退出，学生手动回到竞技大厅时会自动跳回对战答题页。

## 2026-06-04

### 2.2.28
- 极限挑战入口改为先选择单元，再开始该单元的极限挑战；页面布局收敛到和冒险地图接近的选择模板。
- 保持极限规则不变：单元极限仍然是当前单元全部关卡连续挑战，双单元仍然是双单元连考，答错即止。

## 2026-06-04

### 2.2.27
- 学生端与教师端前端可见图标全面去掉 emoji，统一改为矢量图标、字母徽标或文本化排名标识，视觉风格更一致。

## 2026-06-04

### 2.2.26
- 学习记录页的荣誉徽章区去掉 emoji，改为统一风格的矢量图标，整体观感更干净。

## 2026-06-04

### 2.2.25
- 学习记录页的“最近错题”改为卡片内独立滚动区域，长列表不再持续把整页撑长，浏览和回看更稳定。

## 2026-06-04

### 2.2.24
- 修复学习记录页正确率显示错误：后端已返回百分比值时，前端不再重复乘以 `100`，避免出现 `7500%` 这类异常数字。

## 2026-06-04

### 2.2.23
- 成就墙正式接入真实后端数据：服务端现在会自动补齐默认成就定义，并根据答题、闯关、练习和极限挑战进度实时评估解锁状态。
- 学生端成就页补充“最近解锁”提示和解锁时间展示，成就卡片不再长期停留在空白占位状态。
- 普通答题结算接口现在会返回本次新解锁的成就摘要，方便前端后续做即时提示。
- 修正选择题判题兼容性：当正确答案存的是选项文本而学生提交的是选项字母时，也能正确判定。

## 2026-06-04

### 2.2.22
- 新建并补全一套开发基线文档，覆盖产品、架构、接口、数据库、UI 与 AI Agent 协作规则。
- 明确学生端和教师端模块边界，以及前端、后端、数据库、UI、测试各自的职责范围。
- 补充本地 `.ai` 协作文档和角色分工，方便后续 AI Agent 按规则接手开发。

### 2.2.21
- 清理公开预览分支中的历史构建产物，不再保留 `dist/` 下导出的正式版 HTML。
- 修正 `打包部署说明.md`，同步当前实际启动方式、管理员账号、管理端路径和打包建议。

## 2026-06-04

### 2.2.20
- 补充 `.gitignore`，将本地 AI 协作规则目录和临时草稿文件纳入忽略，减少无关未跟踪文件干扰。

## 2026-06-04

### 2.2.19
- 整理 `AGENTS.md`，收口为“协作规则 + 长期约束 + 高频事实”的总纲结构。
- 明确 `docs/PRD.md` 和 `docs/ARCHITECTURE.md` 分别作为产品需求与技术架构的主源文档，减少重复维护。
- 清理 `AGENTS.md` 中与文档重复的大段产品/架构描述，同时保留验证习惯、PVP 约束、视觉规则和 Windows 环境坑位。

## 2026-06-04

### 2.2.18
- 新增 `docs/ARCHITECTURE.md`，整理当前前后端一体化项目的真实技术架构、入口、路由、状态与部署关系。
- 新增 `.ai/rules.md`，补充一份面向 AI 协作者的仓库执行规则，统一修改、验证与文档同步约定。
- 修正文档口径，明确教师端 PVP 当前仍在 `AdminView.vue` 内通过内部 tab 和独立 admin WebSocket 管理。

## 2026-06-02

### 2.2.16
- PVP 对战答题页 UI 重构：从暗色卡片布局改为 iOS 浅色答题页面，左右分栏，状态栏统一顶部。
- 选项按钮改为圆形字母标识，支持选中/正确/错误三态视觉反馈。
- 答题反馈移至右侧独立卡片，展示正确/错误 badge、正确答案和积分变动。

### 2.2.15
- 首页 UI 重构：从暗色 hero 布局改为 iOS 浅色风格，顶部居中品牌标题，主体左右分栏。
- 主功能入口改为白底列表卡片（彩色图标 + 标题描述 + 箭头），辅助入口整合进同一卡片底部。
- 右侧新增账号面板：用户信息、今日推荐 CTA、快速入口、更多操作、退出登录。

### 2.2.14
- 冒险结算页 UI 重构：从单列居中布局改为左右分栏 iOS 风格结果页，左侧结算卡片 + 右侧 2×2 统计区。
- 结算卡片新增绿色完成图标，统计卡片顶部带四色识别条（积分蓝/题数绿/连击橙/正确率红）。

### 2.2.13
- 学生端 PVP 房间准备页 UI 重构：从三列游戏大厅布局改为左右分栏 iOS 风格控制台。
- 房间摘要卡片合并展示房间号、状态标签、当前人数和对战设置，信息更紧凑。
- 房间成员改为 iOS 列表风格，新增空位占位行，清晰展示已加入/待加入玩家状态。
- 右侧集中放置"我的状态"数据格、操作按钮和实时排名表格，层级更清晰。
- 准备按钮作为页面最突出主操作，退出房间改为弱化危险按钮。

## 2026-05-29

### 2.2.12
- 版本号提升到 `2.2.12`，后端 API 版本和启动脚本显示已同步更新。
- 新增单文件打包规格 `pyinstaller_onefile.spec`，可生成把运行时、依赖和前端资源一起封进单个 `exe` 的发布包。
- 保留原有目录式打包 `pyinstaller.spec`，方便在“单文件便携”与“目录式替换”之间按场景选择。

### 2.2.11
- 版本号提升到 `2.2.11`，后端 API 版本和启动脚本显示已同步更新。
- 调整学生端 `PVP` 实时同步策略：任一成员提交答案后，后端现在会主动向同房间所有成员推送各自最新的 `pvp_battle_session` 快照，而不再只广播房间列表状态。
- 这样右下角即时排名、个人当前题目状态，以及“最后一人答完后自动进入结算”都会随着这次提交直接同步到同房间其他成员。

### 2.2.10
- 版本号提升到 `2.2.10`，后端 API 版本和启动脚本显示已同步更新。
- 修复学生端 `PVP` 战斗页右侧即时排名仍偶发停在旧状态的问题：战斗页现已主动请求 `pvp_battle_session` 实时快照，不再只依赖被动房间广播。
- 修复战斗页同步过程误重置当前题目本地起始时间的问题：同一题目的后台同步不再刷新计时，避免同步越频繁，前端本地耗时越失真。
- 保留低频 HTTP 兜底同步，用于在 `WebSocket` 请求快照异常时继续收口最终排名和自动结算状态。

### 2.2.9
- 版本号提升到 `2.2.9`，后端 API 版本和启动脚本显示已同步更新。
- 修复学生端 `PVP` 战斗页名次偶发不刷新的问题：战斗页在保留 `WebSocket` 推送的同时，新增轻量定时同步兜底，其他成员答题导致的战力和名次变化会自动补齐，不再依赖手动刷新。
- 修复“全部成员已完成但战斗页不自动进入结算”的问题：当房间结束状态没有及时由推送送达时，战斗页现在也会主动拉取最新房间状态并自动跳转结果页。

### 2.2.8
- 版本号提升到 `2.2.8`，后端 API 版本和启动脚本显示已同步更新。
- 修复教师端 `PVP` 实时排名不同步的问题：管理页现在会使用管理员令牌接入现有 `WebSocket` 推送链路，房间成员战力、答题计数和最终名次变更无需手动刷新即可同步。
- 现有 `PVP` 自动结算规则保持不变：当整场总时长结束，或全部成员完成答题后，后端仍会统一自动结算并广播最终排名。

### 2.2.7
- 版本号提升到 `2.2.7`，后端 API 版本和启动脚本显示已同步更新。
- 打包链路补充了默认题库导入支持，便于现场演示和快速初始化。

### 2.2.6
- 版本号提升到 `2.2.6`，后端 API 版本和启动脚本显示已同步更新。
- 修复学生端 `PVP` 到时结算仍偶发提前触发的问题：房间状态现已携带 `server_now`，前端整场计时与倒计时统一按服务器时间偏移计算，不再直接依赖本机系统时间。
- 收口 `PVP` 计时显示和后端结算判定的一致性，减少“前端已到 0 秒，但后端还未到总时限”的时间漂移现象。

### 2.2.5
- 版本号提升到 `2.2.5`，后端 API 版本和启动脚本显示已同步更新。
- 收口 `PVP` 总时限结束语义：房间超时判定现在直接按 `battle_expires_at` 生效，不再因为额外宽限导致前端显示 0 秒但后端仍拒绝结算。
- 加固 `PVP` 超时后的答题入口：如果总时限已到，后端会先结束房间并广播最新状态，再拒绝迟到的答题请求，避免“到点后还能交题”或“到点后无法结算”的分叉状态。
- 补充回归测试，覆盖“刚到总时限即可结算”的场景。

## 2026-05-28

### 2.2.4
- 版本号提升到 `2.2.4`，后端 API 版本和启动脚本显示已同步更新。
- 修复学生端 `PVP` 实时分数榜不同步的问题：登录恢复、登录、注册和密码更新后现在都会自动重建 `WebSocket` 认证连接，其他在线成员可以及时收到答题后的房间状态广播。
- 修正学生端 `WebSocket` 心跳和认证状态判断：前端现已发送服务端识别的 `heartbeat`，并按 `authenticated` 状态维护连接，减少“别人答题后必须手动刷新才看到分数变化”的情况。

### 2.2.3
- 版本号提升到 `2.2.3`，后端 API 版本和启动脚本显示已同步更新。
- 修复学生端实时对战分数不同步的问题：登录恢复后现在会自动建立 `WebSocket` 连接，答题后的房间状态广播可以及时推送到其他在线成员。
- 修正学生端 `WebSocket` 心跳和认证状态处理，前端现已发送服务端识别的 `heartbeat`，并按 `authenticated` 状态维持连接可用性，减少“必须手动刷新才看到别人分数变化”的情况。

### 2.2.2
- 版本号提升到 `2.2.2`，后端 API 版本和启动脚本显示已同步更新。
- 修复 `PVP` 自动开战后前端把 UTC 时间当成本地时间解析的问题：房间时间现在统一输出带 `+00:00` 的 UTC 字符串，前端也兼容旧的无时区时间。
- 加固学生端结算接口：未到总时限且仍有未答题时，后端会拒绝提前结算请求，避免客户端倒计时误判把整场对战提前推到结算页。
- 补充回归测试，覆盖 `PVP` 时间序列化和“客户端误触发提前结算”两个场景。

### 2.2.1
- 版本号提升到 `2.2.1`，后端 API 版本和启动脚本显示已同步更新。
- 修复 `PVP` 全员准备后自动开战的时间初始化问题：倒计时自动开始现在会正确写入 `battle_started_at`、`battle_expires_at` 和答题宽限时间，避免刚开战还未答题、未到总时限时被提前结算。
- 教师手动开始和学生全员准备自动开始已统一复用同一套战斗时间冻结逻辑，减少两条启动路径状态不一致。
- 补充后端单元测试，覆盖“自动开战后未答题且未超时不能结算”的回归场景。

### 2.2.0
- 版本号提升到 `2.2.0`，服务启动横幅和启动脚本已统一显示为 `v2.2`，便于明天直接作为第一版测试展示。
- 新增 `80` 端口打包产物 `dist/PythonAdventure80/PythonAdventure80.exe`，默认启动即监听 `http://localhost/`，适合现场双击直接运行。
- 打包链路已补齐运行所需资源：内置 `frontend/dist`、演示数据库 `data/app.dev-copy.db`，并在启动时自动补建缺失数据表，降低首次运行失败概率。

### 2.1.31
- 修复 `PVP` 重新开新一轮时误复用旧会话的问题：现在只会复用“当前这一轮 battle_started_at 对应”的会话，不会再把上一轮已经完成的答题记录带进新一轮，避免一进场就直接完成并被扣满未作答分。
- 学生端房间准备页补上可见倒计时：全员准备后会显示开战倒计时条幅和秒数提示，不再只靠状态文字变化。
- 倒计时结束后学生端会自动从房间准备页跳转到对战作答页，不需要再手动点击“进入对战”。
- 补充 `PVP` 房间状态广播：准备、取消准备、退出房间、教师开始和教师结束对战后，相关成员会立即收到最新房间状态，倒计时和自动跳转更及时。

### 2.1.30
- 收口 `PVP` 整场结束判定：当全部成员完成答题，或整场总时长到点后，房间会立即切换为 `finished`，并自动进入大厅的“对战历史”而不是继续停留在“进行中”。
- 学生端 `PVP` 战斗页新增整场计时条，顶部同时显示总时长与剩余时间；时间归零后会立刻停止作答并触发自动结算。
- 调整学生端 `PVP` 结算流转：个人做完题后不再提前跳“最终结算”，而是停留在“等待房间结束”状态，直到整场房间真正结束再统一进入结果页。
- 即时排名改为统一按后端战力规则实时重排，并补上升降动画与过渡效果，答题后名次变化会直接在榜单中高亮反馈。
- 补齐一轮页面动效一致性：`page-surface`、`surface-panel`、`surface-card`、`metric-card` 等通用面板加入统一入场与悬停过渡，减少“有些地方有动画、有些地方没有”的割裂感。

### 2.1.29
- 修复学生端把判断题误显示成填空题的问题。冒险作答、随机练习、极限挑战和 PVP 对战现在都会把判断题作为独立题型展示，提供“正确 / 错误”两项选择。
- 后端判断题判题兼容旧题库答案格式，现已同时接受 `A/B`、`正确/错误`、`对/错`、`True/False` 等等价写法。
- 题目反馈里的正确答案展示也已同步收口，判断题不再只显示原始字母 `A` 或 `B`。
- 验证通过：`frontend` `npm run build` 成功；判断题判题兼容逻辑已完成代码级验证。

## 2026-05-27

### 2.1.28
- 修复填空题存在多个正确写法时只能命中一种的问题，后端现在支持用分号分隔多个可接受答案，例如 `指令;语句` 会同时接受这两种填写。
- 随机练习的本地判题逻辑同步支持这套多答案填空规则，避免练习模式与正式闯关判题结果不一致。
- 验证通过：`指令;语句` 的判题已实测为 `指令=True`、`语句=True`、`命令=False`，并且 `frontend` `npm run build` 成功。

### 2.1.27
- 修复学生端把历史多选题显示成单选题的问题：冒险作答、随机练习、极限挑战三种学生作答页现已支持真正的多选交互。
- 后端新增“有效题型”兜底逻辑：即使数据库里存在旧数据把多选题误写成单选题，只要答案是多项字母组合且存在选项，接口返回时也会自动按“多选题”处理。
- 已将当前数据库中的历史错标题目批量修正，原先 `50` 道“答案为 ABC/ABD 等多项，但题型为选择题”的题目现已统一改为 `多选题`。
- 验证通过：`frontend` `npm run build` 成功；后端 `backend.app.main` 导入成功；当前数据库中此类错标题目数量已清零。

### 2.1.26
- 新增"多选题"题型支持：后端 question_service 加多选答案校验（排序字母比较）、Excel 导入保留多选类型、管理员题目表单加多选题选项。
- 学生端 PVP 对战：三种题型有独立视觉标识（🔤 单选题紫色 / ☑️ 多选题橙色 / ✏️ 填空题绿色），多选题用 checkbox 多选 + 勾选计数。

### 2.1.25
- 修复 PVP 答题反馈闪现问题：答完后题目立即切换到下一题，无法查看当前题的答案反馈。改为延迟切换 — 后端返回下一题先暂存，等用户点击"下一题"后再替换。

### 2.1.24
- 修复 PVP 答题时"对战会话不存在"错误：`get_my_pvp_battle` 改用 `ensure_pvp_battle_session` 后缺少 `db.commit()`，导致创建的会话在请求结束时被回滚。补上 commit，确保会话持久化到 DB。：`_load_pvp_session_context` 加 fallback — 如果前端传了过期 play_session_id，自动查找当前房间的活跃会话，避免因并发废弃导致答题失败。

### 2.1.23
- 学生端支持自助创建 PVP 房间：大厅新增"创建房间"按钮 + 弹窗表单（名称、人数、题数、时限、出题单元）。
- 学生端支持加入他人房间：活跃房间卡片新增加入按钮（非自己的、未满、等待中的房间）。
- 后端新增 `POST /api/pvp/rooms`（学生建房间）和 `POST /api/pvp/rooms/{id}/join`（学生加入），含冲突校验（不能同时加入两个房间）。

### 2.1.22
- 教师端 PVP 房间列表按状态分组：未开始 / 正在对战 / 已完成，已完成默认折叠，点击标题栏展开。
- 教师端已完成房间右侧详情加"本场排名"标题 + 🥇🥈🥉 奖牌 + 正确率显示。
- 教师端"时限"统一改为"总时限（秒）"并加 placeholder "0 = 不限时"，明确是整场总时限而非每题时限。
- 学生端竞技大厅：已结束房间收入"对战历史"折叠面板，点"刷新大厅"旁边的按钮切换。
- 学生端已结束房间可点击展开内联详情：最终排名 + 战力 + 正确/错误数 + 正确率，当前用户高亮。

### 2.1.21
- PVP 代码审查修复：统一选题逻辑（去内联重复实现，用 `ensure_pvp_battle_session`）、统一成员序列化（去 `_serialize_members`，复用 service 层，补 `is_online`/`accuracy` 等字段）、修复前端类型断裂、排名竞态加时间戳防护、对战限时倒计时、去掉 PvPBattleView 开发者文案、提取共享 `utcnow`/`as_utc` 工具函数、倒计时器改用递归 `setTimeout`。

### 2.1.20
- 恢复消失的页面动画：页面切换 fadeUp 过渡、模态框 scaleIn 入场、补 `spin`/`pulse` keyframe、加 `prefers-reduced-motion` 支持。

### 2.1.19
- 修复 PVP 实时排名不更新的问题：后端答题后广播房间状态到所有成员，前端同步本地排名+战力，加 tiebreaker 排序（战力→正确数→错误数→座位号）。

### 2.1.18
- 修复 PVP 对战获取题目时 `FOREIGN KEY constraint failed` 错误：`PlaySession.level_id` 改为可空（PVP 跨多关卡无单一级别），创建 PVP 会话时设 `None` 替代无效的 `0`。

### 2.1.17
- 修复 PVP 房间全员准备后不自动开始的问题：`update_my_pvp_ready` 设置了 5 秒倒计时但从未调用 `schedule_room_auto_start` 调度后台自动启动任务，导致倒计时到期后房间停留在 `countdown` 状态。

### 2.1.16
- 旧前端 `dev/` 已做历史归档整理，并与当前主线实现分离。
- 清理 `pyinstaller.spec` 中已废弃的 `dev` 打包引用，构建仅依赖 `frontend/dist/`。
- 架构、关键文件、测试与部署说明已对齐 `backend/` + `frontend/` 当前布局。

### 2.1.15
- 教师端模态框加上 `max-height` 与 `overflow-y: auto`，防止 PVP 房间表单等长内容撑爆视口、完全遮挡屏幕。

### 2.1.14
- 教师端 `PVP` 房间表单不再要求老师手填学生 ID 和单元 ID；现已改成可直接勾选的多选面板。
- 参赛学生选择器支持按昵称、用户名和 ID 筛选，并提供“全选当前结果 / 清空”操作，适合批量挑选对战成员。
- 出题单元选择器同样改成真实单元列表，可按名称、说明和 ID 筛选，保存时直接提交选中的单元范围。

## 2026-05-26

### 2.1.13
- 将站点 logo 从带棋盘底的临时 PNG 替换为真正透明背景的 SVG 品牌资产，避免白底页面和 favicon 出现“伪透明”底纹。
- 学生端首页、学生端顶部壳层、登录页、教师端登录页、教师端侧栏和浏览器标签图标现在统一使用同一套品牌 logo 资源。
- 新增前端品牌常量与可复用 `BrandLogo` 组件，后续再调整品牌图形或中英文名称时不需要分别改多个页面。
- 验证通过：`frontend` `npm run build` 成功，构建产物中已不再包含之前 800KB 的带底 PNG logo。

### 2.1.12
- 修复教师端偶发“已经进入后台但首页、学生和题库都没有数据”的问题。
- 原因是旧静态教师端遗留的本地会话 key 与新 Vue 教师端不一致：浏览器里可能残留 `admin_user` 和旧版 `admin_jwt_token`，新前端只识别 `admin_token`，导致页面把自己当成已登录但实际没有可用令牌去拉数据。
- 教师端现在会自动兼容并迁移旧的 `admin_jwt_token` 到新的 `admin_token`；如果浏览器里只剩下孤立的 `admin_user` 而没有任何 token，会直接清理并回到登录页，不再进入空后台。
- 验证通过：已分别模拟“仅旧 token + user”和“仅 user 无 token”两种场景，前者能直接恢复题库与仪表盘数据，后者会正确显示登录页。

### 2.1.11
- 教师端 Vue 管理后台按全新工作台方向重建：统一切到理性专业蓝配色、左侧主导航、中高密度信息布局，不再沿用学生端紫色按钮和旧静态页的装饰式拼贴。
- 教师端首页改成三层结构：首屏 KPI 总览、趋势与单元分析、快捷入口与重点学生提醒；图表改为内置 SVG 折线与条形对比，不再额外引入图表依赖。
- 学生管理、题库管理、排行榜、教学分析、错题统计、PVP 管理和导入中心全部重排为“工具栏 + 主工作区 + 详情侧板/任务面板”结构，补齐学生详情侧板、PVP 房间编辑以及题目真正按关卡选择的表单流。
- 教师端全局样式变量独立收口：输入框、按钮、卡片、表格、状态标签现在全部使用专属 admin token，不再继承学生端首页视觉语言。

### 2.1.10
- 修复学生端首页偶尔仍显示旧版卡片墙的问题：`/app` 的 SPA 入口页现在返回 `Cache-Control: no-store`，浏览器不会继续复用过期的旧 `index.html`。
- 保留哈希静态资源的正常缓存策略，只对入口 HTML 禁用缓存，避免每次改完前端后还看到旧首页结构。
- 验证通过：后端测试请求已确认 `/app/` 返回 `Cache-Control: no-store, max-age=0`。

### 2.1.9
- 学生端首页重新整理为两段式布局：上半区拆成欢迎信息 + 学习概览 + 账号操作侧栏，下半区再按“主要入口 / 辅助入口”分组，不再把所有卡片硬压成同一屏里的单层卡片墙。
- 主模式卡片改成更宽松的双列桌面布局，保留移动端单列回落；学习记录、成就、排行榜和账号提示单独归到辅助区，层级更清楚。
- 学生端通用壳层和公共内容容器同步收紧：`AppShell` 顶栏更稳定，`page-shell` / `page-surface` / `surface-panel` 的宽度、留白和小屏规则统一优化，子页面在平板和手机上更容易展开。

### 2.1.8
- 教师端入口正式切到新前端：后端不再挂载 `dev/admin` 旧静态管理页，避免新旧两套老师端并存造成混淆。
- 兼容入口 ` /admin ` 现在统一跳转到新的 Vue 管理端 ` /app/admin `，老师只会进入新框架页面。
- 根路径 ` / ` 改为进入学生端 ` /app/ `，与当前 2.0 前后端拆分结构一致。
- 验证通过：`backend.app.main` 导入成功，`start.bat` 启动链路通过。

### 2.1.7
- 启动前新增 `backend.app.main` 导入探测，损坏环境会被静默跳过，不再先打印一整段 `greenlet`、`pydantic`、`sqlalchemy` 的导入栈再回退。
- 当两个现有环境都不可用时，脚本现在会用仓库内独立缓存目录 `.tmp-uv-cache-startup` 对 `.uv-backend-venv` 执行 `uv sync --locked --reinstall` 自愈重装，然后再启动服务。
- 验证通过：在全新 `uv` 缓存下重建 `.uv-backend-venv` 成功，`start.bat` 启动链路与 `backend.app.main` 导入检查通过。

### 2.1.6
- 重新压缩学生端首页布局，目标改为桌面端开屏一屏看全：缩短顶部标题栏、统一四个主入口卡片高度，并压缩下排功能卡与账户卡尺寸。
- 调整首页方块比例和内部留白，减少“块太大、内容挤不下”的问题，主入口卡与功能卡现在更接近同一视觉体系。
- 实测预览已按桌面视口检查首页完整展示，不再需要先向下滚动才能看到全部导航内容。
- 验证通过：`frontend` `npm run build` 成功。

### 2.1.5
- 清理学生端泄漏出来的开发说明式文案，移除“学生导航首页”“第一屏只负责导航”“刷题三段式”“PVP 四段式”等非产品化表达。
- 首页、冒险地图、练习、极限挑战、竞技大厅、个人中心等学生可见页面文案统一改回面向学生的正式产品文案。
- 将这条约束写入项目长期记忆：学生端默认禁止出现提示词、实现说明、重构说明、信息架构说明等开发者视角文字。
- 验证通过：`frontend` `npm run build` 成功。

### 2.1.4
- 学生端重构为真正的导航首页：顶部不再只是零散按钮，而是改成推荐入口、主模式块（冒险 / 练习 / 极限 / PVP）、账户块和次级入口（记录 / 成就 / 排行榜）的分层卡片布局。
- 学生端新增独立个人中心页，首页账户卡只保留摘要；学生现在可以在前端修改昵称、修改密码并统一从个人中心退出登录。
- 后端新增学生资料更新接口 `PUT /api/auth/profile`，前端 `useAuthStore` 同步补齐昵称更新与密码更新后的会话刷新逻辑。
- 学习流从旧的 `/game` 巨型单页拆成分场景路由：冒险地图 / 作答 / 结果、随机练习入口 / 作答 / 总结、极限挑战入口 / 作答 / 结果。
- PVP 学生端拆成大厅 / 房间准备 / 对战 / 结算四段式页面；大厅新增房间列表视图与“我的当前房间”推荐区，房间页负责准备与成员状态，对战页改成题目主视区 + 固定排行侧栏，结算页负责个人与房间排名收口。
- 后端补充学生侧只读房间列表接口 `GET /api/pvp/rooms`，用于大厅展示当前开放房间状态。
- 验证通过：前端 `npx vitest run --config vitest.config.ts` 5/5 通过，前端 `npm run build` 成功，后端 `backend/.uv-backend-venv` 环境下 `python -m pytest tests/integration/test_auth.py -q` 4/4 通过。

### 2.1.3
- 修复学生端选择题判错问题：当题库正确答案存为 `A/B/C/D` 而前端提交的是选项文本时，系统现在会把选项字母和选项文本映射到同一选项后再判题，不再出现“明明点对了却判错”的情况。
- 普通闯关与 `PVP` 对战共用这套新判题逻辑，避免两个模式分别出现同类误判。
- 学生端选择题提交流也同步收口为优先提交选项字母，减少前后端答案格式不一致带来的歧义。
- 验证通过：`uv run --project backend python -m pytest backend/tests/unit/test_question_answer_matching.py -q` 4/4 通过，`frontend` `npm run build` 成功。

### 2.1.2
- 修复 Windows 启动脚本 `start.bat`：不再硬依赖单个已损坏的 `venv` 解释器路径。
- 验证通过：`start.bat` 已实测可在旧 `venv` 失效时自动回退并成功启动 FastAPI 服务。

### 2.1.1
- 学生端 Vue 页面按旧静态前端重新迁移版式：首页、登录、冒险地图、随机练习、极限挑战、学习记录、成就、排行榜、PVP 大厅与 PVP 对战统一切回旧版的卡片化学习界面语言，不再维持此前简化过头的毛坯布局。
- `HomeView` 恢复旧版信息架构：顶部品牌栏、三张主模式卡片（开始冒险 / 随机练习 / 极限挑战）、次级功能入口（记录 / 成就 / 排行榜 / 竞技场）以及账号状态面板。
- `GameView` 重做为旧版学习流：单元列表 + 关卡面板、题卡式答题界面、练习解析区、极限挑战选择与结果页，并修正极限模式最后一题后不能推进下一挑战段的问题。
- `RecordsView`、`AchievementsView`、`LeaderboardView`、`PvPLobbyView`、`PvPBattleView` 全部补回更完整的版式层次，恢复总览指标、分区卡片、榜单行、竞技场概览和对战反馈，而不是之前的简陋数据列表。
- `AppShell` 顶部外壳同步升级：加入品牌标识、当前账号展示、首页/登录/退出入口，学生端各子页面的头部表现重新统一。
- 排行榜前端改为支持匿名访问，未登录用户也可直接查看公开榜单；组件测试补充 router 注入以匹配新页面结构。
- 验证通过：`npm run build` 成功，`npm test` 3/3 通过。

## 2026-05-25

### 2.1.0
- 全局样式从暗色主题切换为双 CSS 变量浅色方案（学生端 :root + 教师端 [data-theme="admin"]）
- 教师端改为白底专业后台：深蓝灰侧边栏、白色卡片、蓝色强调色、去装饰化
- 学生端首页重写为导航启动台：双排六卡片（开始冒险、学习记录、用户 + 竞技大厅、排行榜、成就）
- 学生端子页面改为顶部返回栏布局，内容区居中 960px，去掉侧边导航
- 登录页适配浅色主题，游戏/记录/排行/成就/PVP 全部适配新样式
- 清理所有视图中的暗色硬编码背景色，统一改为 CSS 变量引用

### 2.0.8
- 重写首页 HomeView 为学生端导航启动台：双排卡片布局（开始冒险、学习记录、登录/用户卡片 + 竞技大厅、排行榜、成就），浅色主题卡片网格
- 首页显示品牌 Logo "PA" 和标题 "学习对战平台"，用户卡片支持已登录/未登录双态

### 2.0.7
- 启动服务器时自动打开浏览器到管理端页面（`/app/admin`），并打印服务器横幅
- 用 Vue 3 重建首页：`/` 展示闯关首页（标题、行动卡片、底部导航），登录/注册移至 `/login`
- 管理端新增 Excel 题库导入，支持 `.xlsx/.xlsm`，智能识别表头列布局，兼容模板和题目列表两种格式
- Excel 导入按「知识点」列自动分组：每个知识点自动成为一个单元，单元内全部题目放入一个关卡
- 后端新增 `POST /api/admin/import/excel` 端点，依赖 `openpyxl` 解析 Excel 文件

### 2.0.6
- 将老师管理端重新拆分为独立入口：`/app/admin` 现在使用独立登录态、独立侧边菜单和独立后台布局，不再复用学生端页面外壳。
- 按旧版后台结构补回老师端主要模块：仪表盘、学生列表、排行榜、关卡分析、错题统计、题目管理、PVP 管理、题库导入。
- 新增老师端批量导入学生与题库导入前端工作流，并为 2.0 后端补齐对应统计与导入接口。
- 学生端补回更完整的信息架构：新增学习记录页面与导航入口，学习地图页增加记录、排行榜、成就、竞技大厅等功能入口，不再只剩单一关卡视图。
- 学生端登录与老师端登录彻底分离：学生端管理员账号会被引导使用管理端入口，避免两端混用同一登录页。

### 2.0.5
- 学生端与管理端 UI 统一重做为专业化深色数据台风格，收口导航、标题区、卡片、表格、按钮、表单和状态标签视觉语言。
- 重写登录页、学习地图、排行榜、成就、PVP 大厅、PVP 对战页，移除不专业的提示式文案，界面只保留功能信息与操作入口。
- 重写管理台布局与模块呈现，统一学生管理、用户列表、题目管理、竞技房间四个模块的工具栏、数据卡、表格和弹层样式。
- 调整 `AppShell` 与全局样式系统，建立学生端/管理端共用的侧边导航和响应式内容区，避免页面之间风格割裂。
- 调整前端构建入口，显式固定 Vite build root，修复当前 Windows 环境下 `npm run build` 因错误解析临时工作目录而失败的问题。
- 验证通过：`npm run build` 零错误，`npm test` 3/3 通过，并已用浏览器预览复核登录页、学生端主界面和管理端壳层。

### 2.0.4
- 重做 Vue 前端视觉层：新增统一 `AppShell` 应用外壳，补齐导航、标题区、用户信息、背景氛围和统一卡片体系。
- 重写登录页、冒险地图、排行榜、成就墙、PVP 大厅、PVP 对战页样式，修复“已构建但界面极度毛坯”的问题。
- 前端 API 错误处理改为安全 JSON 解析与兜底文案，不再把 `Unexpected end of JSON input` 这类底层异常直接暴露给用户界面。
- 修复管理员页面在刷新后“消失”的问题：认证状态现在同时持久化 `token` 与当前用户快照，并在应用启动时自动通过 `/api/auth/me` 恢复管理员身份。
- 调整组件测试：`AppShell` 在无 router 注入的测试环境下也可独立渲染，`GameView` 组件测试补充 fetch stub，避免本地网络噪声。
- 构建验证通过：`npm run build` 零错误，`npm test` 3/3 通过，并已用 Playwright 对 `/app/` 登录页和 `/app/game` 页面做可视化复测。

### 2.0.3
- 更新 start.bat 指向新 backend/ 目录，使用 uv run 启动 FastAPI 服务。
- 更新 build.py 适配 frontend/dist/ Vue SPA 构建产出与 /app/ 基础路径。
- 更新 pyinstaller.spec 入口改为 backend/app/main.py，datas 改为 frontend/dist/，补齐所有 backend.app.* 隐藏导入。
- 同步整理 2.0.0 全栈重组后的架构说明文档。
- 更新 更新日志/2.0.0.md 为完整重组记录。
- 后端 18 个测试全部通过，前端 vitest 3 个测试通过，vite build 零错误。

### 2.0.2
- 新增 Vitest + Vue Test Utils + happy-dom 单元测试框架，编写 LoginView 和 GameView 组件测试。
- 新增 Playwright E2E 测试骨架（登录页面加载验证）。
- npm 脚本新增 test / test:watch / test:e2e 三条命令。

### 2.0.1
- 创建 frontend/ 项目骨架：Vite + Vue 3 + TypeScript + Pinia + Vue Router，构建产出 50 模块零错误。
- 新增 auth API 封装、Pinia 认证 store、Vue Router 7 条路由及 7 个页面占位组件。

### 2.0.0
- 启动后端重构：创建 backend/ 项目骨架，基于 FastAPI + SQLAlchemy 2.0 + pydantic-settings + uv 包管理。
- 新增配置管理、数据库引擎与双模式会话（HTTP / WebSocket-后台任务）、业务异常体系。
- 迁移全部 14 个 ORM 模型至新 backend，补全双向 relationship、统一 DateTime(timezone=True)、修复 PvpRoomMember.is_ready 类型。
- 更新 .gitignore 适配新项目布局（backend/frontend/data 忽略规则）。

### 1.0.48
- PVP 竞技系统全面加固：修复会话创建时误废弃冒险模式进度的问题，答题接口增加房间状态二次校验防止结算竞态，结算时未答题目自动扣分保证排名公平。
- 教师编辑已结束房间时自动清理战斗时间残留数据，排序逻辑根据 ranking_metric 字段动态选择排序策略。
- 学生新增"退出房间"功能，可在等待/倒计时阶段主动离开竞技房间离开后倒计时自动取消。
- 答题接口错误处理更精确，区分唯一约束冲突与其他数据库异常。

## 2026-05-23

### 1.0.47
- 修复学生端对战超时后可能卡在“结算失败”的问题：前端现在会把宽限窗口冲突和“会话已结束”这类业务状态与真正的网络异常区分处理，不再一律误报成网络失败。
- 对战超时结算遇到服务端宽限窗口尚未关闭时，学生端会自动稍候一次再重试；若服务端已先完成该会话，也会直接进入战绩页，不再要求学生反复手动重试。
- 学生端静态资源版本升级到 `1.0.47`，确保浏览器拉到新的对战结算容错逻辑。

### 1.0.46
- 修复学生端启动即弹出“连接失败”的致命问题：误被清空的 `40_api.js` 已恢复，首页初始化重新具备题库拉取、登录态管理和在线连接能力。
- 补齐学生端密码修改后的登录态刷新链路，`API` 现对外暴露 refresh token 写入方法，避免修改密码后前端因缺少方法再次报错。
- 学生端静态资源版本升级到 `1.0.46`，确保浏览器拉到恢复后的 API 层脚本。

### 1.0.45
- 修复学生端题卡内联结果区的 `hidden` 显示异常：结果容器、错题详情块和 `下一题` 按钮现在会严格按隐藏状态收起，不再被组件样式里的 `display:grid` / `display:flex` 顶出来。
- 学生端静态资源版本升级到 `1.0.45`，样式资源版本升级到 `2.8`，确保浏览器拉到修正后的隐藏逻辑。

### 1.0.44
- 修正学生端题卡内联纠错块的显示逻辑：错题现在会稳定展示“你的答案 / 正确答案 / 错误提示 / 规则提醒”，不再因为样式状态遗漏而整块不显示。
- 收紧题卡内联结果区的交互时机：`下一题` 按钮默认隐藏，只有本题答完并进入结果态后才出现。
- 学生端静态资源版本升级到 `1.0.44`，样式资源版本升级到 `2.7`，确保浏览器拉到修正后的题卡反馈界面。

### 1.0.43
- 学生端答题结果从全屏解析弹窗改为题卡内联反馈；答完后不再打断当前页面节奏，继续按钮直接留在题目下方。
- 错题现在会在题目区域旁边显示紧凑的纠错块，集中提示正确答案、错误原因和规则提醒，避免把整段解析一股脑弹出来。
- 学生端静态资源版本升级到 `1.0.43`，样式资源版本升级到 `2.6`，确保浏览器拉到新的内联答题反馈界面。

## 2026-05-19

### 1.0.42
- 竞技场对战题流从冒险链路中拆出：学生端进入 `PVP` 后不再复用 `/api/records/next-question` 和 `/api/records/answer`，改为走独立的 `PVP` 当前题与答题接口。
- `PVP` 开战时不再一次性把整包题目、标准答案和解析下发到前端；现在只下发当前题，提交后才返回该题答案与解析，并由后端控制下一题推进顺序。
- 收紧对战态约束：`PVP` 会话禁止通过冒险答题接口提交，乱序作答会被拒绝，房间结束后旧对战会话也不能继续答题。
- 教师端不再允许编辑进行中或倒计时中的 `PVP` 房间，避免对战中途重建成员导致实时战力和准备态被清空。
- 学生端静态资源版本升级到 `1.0.42`，确保浏览器拉到新的独立 `PVP` 刷题链路。

### 1.0.41
- 学生端主逻辑文件按功能域拆分：`20_app.js` 只保留核心玩法流，学习记录/错题分析拆到 `21_progress.js`，竞技场/排行榜拆到 `22_arena.js`，密码修改与战力结算拆到 `23_account.js`，降低单文件维护成本。
- `dev/index.html` 的脚本加载顺序同步调整，并升级学生端静态资源版本到 `1.0.41`，确保浏览器拉到新的拆分结构。

### 1.0.40
- 修复竞技场房间在全员准备后只进入 `countdown`、却不会在倒计时结束时主动切到 `running` 并推送给学生端的问题；后端现在会在到点后自动开战并广播房间状态。
- 学生端竞技场补了一个本地到点兜底：如果已在竞技场页且倒计时到零，会主动再请求一次对战题单/房间状态，不再死等下一条推送。
- 额外扫了一轮学生端固定 DOM 节点依赖，未发现更多像 `floatingCoins` 那样“模板已删但全局函数仍硬依赖”的同类问题。
- 学生端静态资源版本升级到 `1.0.40`，确保浏览器拿到新的竞技场自动进战逻辑。

### 1.0.39
- 修复学生端答对题目后漂浮金币/星星特效依赖已删除 DOM 容器的问题；现在会在运行时自动创建特效层，不再因为 `appendChild` 目标为空而中断答题流程。
- 补回漂浮金币与星星的前端样式和动画，确保修复后不只是“不报错”，奖励反馈也能正常显示。
- 学生端静态资源版本升级到 `1.0.39`，样式资源版本升级到 `2.5`，避免浏览器继续命中缺失特效层的旧脚本和旧样式。

### 1.0.38
- 修复学生端 `20_app.js` 在切换页面时调用已删除的 `stopArenaRefresh()` 导致全站导航报 `ReferenceError` 的问题；竞技场改成长连接后保留了兼容清理函数，避免普通冒险、练习、记录、成就、排行榜页面一起被拖死。
- 学生端静态资源版本升级到 `1.0.38`，确保浏览器尽快拉到修复后的 `20_app.js`。

### 1.0.37
- 学生端在线连接改成“页面启动即建立全局 WebSocket，登录后在同一条连接上补鉴权”，不再要求先登录成功才能建立在线通道。
- 竞技场首屏状态和开战取题切到 WebSocket 请求，不再先走 `/api/pvp/my-room` / `/api/pvp/my-battle` 再切到长连接推送。
- 学生端静态资源版本升级到 `1.0.37`，确保浏览器拉到新的常驻连接协议与竞技场实时请求逻辑。

### 1.0.36
- 修复学生端全局在线 WebSocket 在本地残留过期登录态时的重连风暴：认证失败后不再继续拿同一坏 token 重连，登录态清空时也会同步断开 socket。
- 收紧学生端启动时的在线连接条件，只有本地同时存在 access token、refresh token 和用户信息时才会自动恢复在线状态，减少“还没来得及登录就被旧会话拖住”的卡死感。
- 学生端静态资源版本升级到 `1.0.36`，避免浏览器继续命中旧版 `40_api.js` 导致修复不生效。

### 1.0.35
- 竞技场房间状态和实时榜单改为走 WebSocket 推送：教师端 PVP 面板与学生端竞技场不再依赖 3 秒轮询，准备状态、倒计时、开战、答题计分、房间成员变化会即时刷新。
- 修复 `ensure_pvp_battle_session` 的并发竞态：同一学生并发进入同一房间时，后端现在通过数据库唯一约束和冲突后回查复用，避免重复创建多条 PVP `PlaySession`。
- 测试环境同步执行启动迁移，补齐 PVP active session 唯一索引和 WebSocket 推送回归覆盖，避免测试库漏掉运行时原始 SQL 索引。

### 1.0.34
- 修复教师端 PVP 日志时间格式化在收到非法日期字符串时会把原始值直接返回的问题，异常值现在会先转义后再渲染，避免管理端日志区成为 XSS 落点。
- 收紧 PVP 房间题库配置解析：`question_unit_ids` 不再静默吞掉坏数据，遇到损坏 JSON 或非法单元 ID 会明确报错，便于尽快发现被手工改坏的房间配置。
- 回收答题开局限流器中过期用户的内存记录，避免服务长期运行后 `_session_rate_limit` 字典只增不减。

### 1.0.33
- 取消老师创建/导入学生账号时默认开启“强制修改密码”的行为，并在启动迁移中统一清除已有学生账号上的该标记，避免学生首次登录后被无意义地挡在竞技场等在线功能之外。
- 竞技场房间新增 `countdown` 状态：当房间内所有学生都已准备后，会自动进入 5 秒倒计时，并在倒计时结束后自动切换到进行中，不再依赖老师手动点击开始。
- 学生端竞技场等待页重排为“单一房间状态 + 成员头像气泡 + 单按钮准备/取消准备”，只在对战真正开始后才展示实时排名，减少等待阶段界面信息割裂。

### 1.0.32
- 修复强制修改密码账号在学生端提交新密码时可能触发的后端 500 错误。问题原因是部分 `db.execute(...)` 仍使用了 SQLAlchemy 2.x 不接受的裸 SQL 字符串。
- 现在学生改密码、老师后台改密码以及禁用学生时，相关 token 版本更新语句统一改为 `text(...)` 执行，避免接口在关键流程中直接崩溃。

### 1.0.31
- 修正学生端竞技场在“强制修改密码”账号下的错误提示：当后端返回“请先修改密码”时，竞技场不再一半显示真实原因、一半只显示“请稍后再试”。
- 学生端现在会在竞技场阻断态中明确展示原因，并直接提供“去修改密码”入口，减少学生误以为竞技场房间或账号本身损坏的混淆。

### 1.0.30
- 将竞技场从原来的左右小组对战重做为“教师建房 + 多人混战”的房间赛制，老师可直接邀请在线学生进房、查看准备状态并控制开战与结束。
- 新增房间内实时战力榜：学生每次答题后会即时刷新战力、正确/错误统计和排名，学生端不再显示旧播报流，事件日志改为集中显示在教师后台。
- 重排教师端 PVP 工作台与学生端竞技场页面结构，改成房间列表、房间控制区、在线邀请区、实时榜单和教师日志的统一界面；静态资源版本升级到 `1.0.30`，学生端样式资源升级到 `2.3`。

### 1.0.29
- 重做学生端的统一视觉系统，收口首页、单元选择、关卡地图、答题页、记录页、排行榜、竞技场和开发者面板的布局、按钮、卡片与背景语言。
- 修复大量页面类名没有对应样式而回退成浏览器默认控件的问题，减少“像几个不同项目拼在一起”的割裂感。
- 学生端静态资源版本升级到 `1.0.29`，样式资源升级到 `2.2`，降低浏览器继续命中旧缓存导致新旧界面混搭的问题。

### 1.0.28
- 修复学生端首页被结果弹层和开发者口令层默认显示遮住的问题，恢复首次打开即可正常进入主页。
- 清理学生端首页顶部残留的重复状态栏标记，移除会造成布局错乱和重复 ID 的残缺 HTML。
- 学生端样式资源版本升级到 `2.1`，减少浏览器继续命中旧 CSS 缓存导致页面看起来仍然“完全不能用”的问题。

## 2026-05-18

### 1.0.27
- 前端题库全面迁移至后端API，删除静态题库文件（10_unit1_questions.js、11_unit2_questions.js）
- 新增4个后端聚合端点：单元嵌套关卡、批量查题、按单元取题、单题查询
- QuestionOut Schema 新增 level_id 和 unit_id 字段
- 前端新增三层内存缓存，重复进入关卡零API请求，练习模式一次API拉取整单元
- 启动阶段改为异步加载，API不可达时显示错误页+重试按钮
- 游客可正常访问题目API游玩，成就仅登录用户加载

### 1.0.26
- 修正上一轮学生端回退范围过大的问题，重新将学生端恢复到”后续 UI 仍保留、但题库主链路仍走前端静态题库”的基线。
- 当前学生端重新使用 `dev/data/10_unit1_questions.js`、`dev/data/11_unit2_questions.js` 与 `unitQuestionsMap` 作为主取题来源，同时保留记录页、竞技场页等后续学生端界面。
- 学生端静态资源版本升级到 `1.0.26`，用于强制刷新到这次定向回退后的页面和脚本。

### 1.0.25
- 将学生端题库与做题主链路回退到前端静态题库版本，恢复 `dev/data/10_unit1_questions.js`、`dev/data/11_unit2_questions.js` 和旧版题目映射读取方式，用于直接验证数据库接入前后的行为差异。
- 学生端 `index.html`、`00_core.js`、`10_state.js`、`20_app.js`、`style.css` 同步回退到旧版工作基线，先恢复已知可运行路径，再继续定位后续问题。
- 学生端静态资源版本升级到 `1.0.25`，避免浏览器继续命中回退前的页面脚本和样式缓存。

### 1.0.24
- 回退并重构学生端首页的点击层处理，不再依赖多层 `z-index` 硬抬交互区。
- 将首页结构层改为不接管指针事件，只让真实按钮和卡片接收点击，针对“登录/注册、冒险模式、修炼场、做题记录无响应，但排行榜和竞技场还能点”的现象做定向修复。
- 学生端样式资源版本升级到 `1.0.24`，减少浏览器继续命中旧首页点击层样式的问题。

### 1.0.23
- 调整学生端首页交互层级，抬高主按钮、登录按钮和右侧卡片的可点击层，减少首页上半区被透明布局层遮住导致按钮无响应的问题。
- 学生端样式资源版本升级到 `1.0.23`，减少浏览器继续命中旧首页层级样式的问题。

### 1.0.22
- 为学生端首页和教师端首页追加禁缓存响应头，减少浏览器长期复用旧版 HTML 导致继续加载过期脚本版本的问题。
- 这样前端迭代后，根页面和 `/admin/` 页会更快拿到最新的脚本引用，不容易出现“服务端已更新，但浏览器还在跑旧版本”的假象。

### 1.0.21
- 将学生端各页面的“返回主页”统一切到同一套安全回退逻辑，不再只修竞技场单页。
- 现在排行榜、竞技场、记录页、成就页、练习页等返回按钮会先尝试单页切回首页，失败时再整页跳转到根路径。
- 学生端静态资源版本升级到 `1.0.21`，减少浏览器继续命中旧返回按钮逻辑的问题。

### 1.0.20
- 调整 `start_admin.bat` 的定位：保持“默认打开教师端页面”，但仍然启动同一个完整服务，学生端继续可访问。
- 移除教师端专用模式下对根路径的强制重定向，避免测试阶段需要老师端可视化界面时，学生端反而被一起挡掉。

### 1.0.19
- 将 `start_admin.bat` 对应的教师端专用模式进一步收紧：根路径 `/` 现在会直接跳转到 `/admin/`。
- 这样使用教师端专用启动时，不再误打开学生端页面，避免出现“学生端页面能打开但不该在该模式下使用”的混淆。

### 1.0.18
- 修正教师端专用启动入口的静态挂载策略：恢复使用完整静态站点，并默认打开 `/admin/`，避免教师后台资源在专用启动模式下路径错位。
- 这样通过 `start_admin.bat` 运行时，教师端页面仍然优先打开，但 `admin.js`、`admin.css` 等资源会按既有 `/admin/` 路径正常加载。

### 1.0.17
- 修复 `start_admin.bat` 对应的教师端专用入口没有切换到 `admin` 静态模式的问题。
- 现在通过教师端专用启动脚本运行时，会稳定以教师后台静态页作为默认挂载，不再误用学生端静态页。

### 1.0.16
- 调整学生端竞技场返回交互：移除额外退出键，保留“返回主页”作为唯一出口。
- 为竞技场“返回主页”补上安全回退逻辑；当单页切屏失败时，会直接整页返回根路径，避免点击后无反应。
- 学生端静态资源版本升级到 `1.0.16`，减少浏览器继续命中旧缓存导致竞技场页继续使用旧返回逻辑的问题。

### 1.0.15
- 调整学生端竞技场返回交互：移除额外退出键，改为强化原有“返回主页”按钮。
- 为竞技场“返回主页”补上安全回退逻辑；当单页切屏失败时，会直接整页返回根路径，避免点击后无反应。
- 学生端静态资源版本升级到 `1.0.15`，减少浏览器继续命中旧缓存导致竞技场页继续使用旧返回逻辑的问题。

### 1.0.14
- 将学生端早期静态题库正式收口到后端数据库，学生端闯关与修炼场改为按单元/关卡从接口拉取题目，不再依赖前端内置题目脚本。
- 放开题库目录只读接口，未登录状态也能读取单元、关卡和题目内容，保证游客模式在移除静态题库后仍可正常进入做题流程。
- 启动迁移时补种缺失的内置题库：如果默认关卡存在但题目为空，会自动把内置题库写回数据库，减少旧环境升级后“有关卡没题目”的情况。
- 清理旧静态题库文件与历史提取脚本，避免数据库题库与前端内置题库双份维护继续分叉。
- 学生端静态资源版本升级到 `1.0.14`，减少浏览器继续命中旧脚本导致仍在引用旧题库结构的问题。

### 1.0.13
- 调整学生端记录页最近作答与错题预览的来源气泡，压缩为更短的单元/关卡标签，避免标题过长挤占内容区。
- 成就页右上角新增快捷退出按钮，不必再滑到页面底部才能返回主页。
- 重构学生端竞技场界面，改为房间总览、左右阵营对战区和实时播报分栏布局，不再沿用旧的列表式面板。
- 移除学生端“勋章墙”相关文案，统一改为成就进度表达。
- 学生端静态资源版本升级到 `1.0.13`，减少浏览器继续命中旧缓存导致页面样式不更新的问题。

### 1.0.12
- 继续重构学生端组件层，统一按钮、气泡、分段筛选、统计卡、挑战卡、地图节点和榜单面板的视觉与交互规则。
- 优化学生端页面版式，调整首页入口网格、记录页操作条、排行榜头部和竞技场双栏布局，使信息分区更清楚。
- 重做登录页页签、输入框和操作按钮层级，减少“功能有了但界面像临时拼装”的感觉。
- 强化单元卡、关卡节点、题目选项卡和失败页按钮组的布局与点击反馈。
- 学生端静态资源版本升级到 `1.0.12`，减少浏览器继续命中旧缓存导致组件样式不更新的问题。

### 1.0.11
- 重构学生端整体视觉语言，统一首页、登录页、单元选择、关卡地图、排行榜、竞技场和学习档案页的界面风格。
- 学生端首页改为更明确的“学习作战台”布局，强化主线闯关、复盘和竞技场入口层级。
- 重做学生端卡片、按钮、地图节点和榜单面板样式，提升信息分区和可点击反馈。
- 优化学生端移动端布局与减弱动态效果设置，补充 `prefers-reduced-motion` 兼容。
- 学生端静态资源版本升级到 `1.0.11`，减少浏览器继续命中旧缓存导致页面样式不更新的问题。

### 1.0.10
- 重做学生端“做题记录”页，改为数据库驱动的学习档案视图，新增云端同步状态、分单元进度、最近作答和错题聚焦面板。
- 新增学生端学习档案聚合接口，一次返回总览、单元进度、最近作答和错题分析所需数据。
- 学生端错误分析改为直接读取后端答题记录，和教师后台共用同一份数据库历史。
- 补齐旧答题记录的归属兼容处理，避免历史数据在单元/关卡展示时出现异常。
- 学生端静态资源版本升级到 `1.0.10`，减少浏览器继续命中旧缓存导致“页面没变化”的问题。

### 1.0.9
- 重做教师端题目管理：新增单元筛选、关卡筛选、题型筛选、题目统计条，并修复新增/编辑题目流程。
- 修复教师端题目编辑依赖“拉全量再本地查找”的问题，新增单题详情接口。
- 统一题目类型契约：后台创建、编辑、导入同时兼容旧值与当前中文题型展示。
- 修复单元关卡接口缺少关卡标识的问题，教师端现在可以稳定按单元/关卡过滤题目。
- 扩展题库导入：支持自动分关与按关卡分类导入两种格式。
- 新增学生端 PVP 竞技场页，支持查看老师分配的对战分组和最新播报。
- 新增教师端 PVP 管理：可创建/编辑 2-8 人竞技场分组、分配学生，并发布播报。
- 新增教师端独立启动入口 `server/main_admin.py` 与 `start_admin.bat`，可只打开教师后台静态页。
- 新增学生访问 IP 日志，用于局域网部署排障与在线状态诊断。

## 2026-06-04

### 2.2.17
- 新增 `docs/PRD.md`，整理当前综合教育平台的产品需求基线文档。
- 统一梳理学生端、教师端、PVP 对战、部署场景和非功能要求，便于后续开发与交接。
- 明确当前文档口径以“已实现能力 + 现有限制 + 持续迭代重点”为主，降低需求理解偏差。

## 2026-05-16

### 1.0.8
- 完成一轮安全加固，修复认证、输入渲染与错误提示相关风险。
- 移除不安全的开发调试入口，收紧注册开关控制权限。
- 加强学生端与教师端内容渲染转义，降低脚本注入风险。
- 统一登录失败提示，减少账号探测风险。
- 补充安全回归测试，覆盖关键认证与渲染链路。

## 2026-05-15

### 1.0.7

#### 安全与防作弊
- 修复答题 Session 清理逻辑，过期 Session 现在会正确标记为 `expired`，避免重复开局时状态残留。
- 修复状态同步接口过度信任前端进度的问题，改为服务端根据真实答题记录重建关卡进度。
- 强制改密门禁并入统一鉴权链，`force_password_change` 用户现在无法绕过改密直接使用其他接口。
- 调整答题防作弊的最短用时处理：低于 2 秒的作答不再直接拒收，改为按最小 2 秒记账，避免学生端本地通关但后端完全无记录。
- 修复最终一题提交后的 Session 完成判定，确保 Session 会进入 `completed`，连击、关卡战力和在线时长能正确落库并被教师端看到。
- 修复 2 秒保底计时导致的战力误判，速度分和连击分恢复正常计算。

#### 学生端
- 修复开局异步建 Session 的竞态，答题不再因为 `play_session_id` 还没回来而静默丢记录。
- 补齐轻量 toast 提示，替换原来会退化成 `alert()` 的交互。
- 重做学生端首页信息层级与主界面入口布局，提升首次进入时的可理解性。
- 学生端登录页现在会持久化显示登录状态；刷新后会恢复账号显示，并按注册开关隐藏或展示注册入口。
- 重做学生端排行榜 UI，增加公开访问提示、清晰表头和个人名次区。
- 排行榜改为匿名可见：未登录学生也能直接查看战力榜和在线时长榜，登录后才额外展示个人名次。
- 将原“周活跃榜”统一调整为“在线时长榜”，后端按有效答题记录的累计 `time_spent` 汇总，学生端结算与榜单文案同步更新。
- 关卡结算补齐战力明细面板，显示本关战力、总战力、在线时长和四维分项，不再额外弹独立结算窗。

#### 教师端
- 重做教师端整体视觉主题、仪表盘头部和登录卡样式，并把学生列表操作改为事件委托。
- 新增学生端自助注册开关：后端默认保留能力但可关闭，教师端仪表盘可控制状态。
- 新增学生批量导入接口和教师端导入入口，支持按 `username/nickname/password` 批量创建学生账号。
- 新增题目 `title` 字段并接入题目管理、错题统计和导入规范，教师端不再只能靠题干长文本定位题目。
- 扩充教师端仪表盘数据分析，新增薄弱单元、高表现学生、待跟进学生、推进率与练习强度等细分指标。
- 重做教师端登录门禁：后台页面默认以未认证状态渲染，登录前直接隐藏侧栏和全部管理内容。

#### 排行榜与后端稳定性
- 修复选项 JSON 解析兼容性问题，避免题目配置在部分场景下崩溃。
- 修复排行榜按昵称判定“我的排名”的错误，改为按 `user_id` 匹配，避免同名用户串榜。
- 修复在线时长榜接口响应模型不匹配导致的加载失败。
- 修复学生正常通关后排行榜战力仍为 0 的问题：关卡完成时立即回写 `UserStats.power_score`。
- 移除战力计算服务里的隐式 `db.commit()` 副作用，避免读取型接口提前提交事务。

#### 启动与部署
- 调整 `start.bat` 启动提示：默认端口仍为 `80`，并在启动时明确打印游戏页和教师端地址，便于区分当前访问地址。
- 新增 `start.bat` 启动脚本，提升 Windows 本地启动稳定性。

## 2026-05-14

### 1.0.6
- 新增备用端口 8000 的打包配置，生成 `game_8000.exe`，无需管理员权限即可启动。
- `server/main.py` 重构：启动逻辑提取为 `run_server()` 函数，支持通过 `GAME_PORT` 环境变量自定义端口。
- 新增 `server/main_8000.py` 入口，默认端口 8000。
- 新增 `pyinstaller_8000.spec` 和 `build_8000.bat`，打包输出到 `dist/game_8000/`。
- 新增关卡制战力值系统（四维得分：通关/完美/速度/连击）。
- 新增战力总榜和在线时长榜（原周活跃榜），支持双榜切换和 `my_rank`。
- 新增答题 Session 管理（start-session → next-question → answer），带时序校验和幂等保护。
- 新增防作弊系统：session 过期检测、答题时间下限、时序交叉验证、combo 重算。
- 新增修改密码功能（Token 版本化 + WebSocket 主动踢出）。
- 新增学生账户管理（创建/编辑/禁用/恢复），支持 `include_disabled` 筛选。
- 新增 WebSocket 鉴权缓存（10s TTL），减少 DB 压力。
- 新增 SessionQuestion / PlaySession / ScoreReviewDecision 三张数据表。
- 新增 `server/migrations.py` 版本化迁移和战力值历史数据迁移。
- 开启 SQLite WAL 模式，提升并发性能。
- 教师后台新增学生 CRUD UI，前端新增排行榜、战力结算、修改密码界面。

### 1.0.5
- 修复同一机器切换账号后，新账号会获取上一个账号本地缓存进度的问题：退出登录时自动清除本地游戏存档。
- 登录成功后改为从服务端拉取进度数据覆盖本地缓存，不再直接推送本地数据到服务端，防止跨账号数据串扰。
- 服务端有数据时（回归用户），自动恢复关卡星级、解锁状态、统计数据、已解锁成就及错题记录。
- 服务端无数据时（新用户），本地使用默认空白进度，不会带入其他账号的离线缓存。
- 成就拉取不再依赖服务端是否有答题数据，仅凭成就本身即可判断是否为回归用户。
- 成就同步入库：每次同步状态或提交答案后，服务端自动根据统计数据评估成就并写入 `UserAchievement` 表，支持换机恢复。

### 1.0.4
- 成就弹窗从屏幕居中移至右下角，不再遮挡答题题目，沿用现有淡入淡出动画体系。
- 成就弹窗支持多弹窗堆叠，最多同时显示 3 个，超出时自动移除最旧的，各弹窗垂直排列互不重叠。
- 新增 WebSocket 实时在线状态系统：学生登录后自动建立 WebSocket 每 30 秒心跳，关闭页面自动离线。
- 教师后台仪表盘新增“当前在线”卡片，实时显示在线人数/总人数，点击可展开在线/离线学生名单。
- 教师后台学生列表新增“状态”列，每行学生昵称前显示绿色（在线）或灰色（离线）圆点。
- WebSocket 连接支持指数退避自动重连，网络中断恢复后无需手动刷新。
- 后端新增 `/ws/online` 端点，Token 通过首条消息传递，避免密钥泄露到服务器日志。

### 1.0.3
- 修复游戏前端登录后仍主要使用本地 `gameState` 与 `localStorage`、没有把学习进度同步到数据库的问题。
- 新增状态同步接口，登录后与本地保存时会把总答题数、总分、连击、极限测试次数、单元关卡解锁和星级同步到后端。
- 调整游戏前端答题流程：已登录用户每次答题都会实时提交 `AnswerRecord` 到后端，教师后台与数据库统计不再长期脱节。
- 调整教师后台仪表盘总答题数统计，优先兼容已同步到 `UserStats` 的累计答题数据。

## 2026-05-13

### 1.0.2
- 修复教师后台左侧菜单在自动恢复管理员登录态后无法切换页面的问题。
- 调整教师后台初始化顺序，先绑定侧边栏导航事件，再恢复登录态，避免页面切换失效。
- 为教师后台静态资源追加版本参数，避免浏览器继续复用旧版 `admin.js` / `admin.css` 缓存。

## 2026-05-12

### 1.0.1
- 修复 `dev/index.html` 与 `build.py` 打包协议不一致的问题，恢复 `BUILD` 占位符，确保发布版 HTML 会内联样式和脚本。
- 重新生成发布文件，修复打开 `dist` 目录中的正式版页面时样式缺失的问题。

## 2026-04-21

### 1.0.0
- 新增开发者口令验证弹层，统一开发者模式入口。
- 新增开发者控制台总览卡片，可快速查看积分、做题数、星级、成就和极限测试统计。
- 新增按单元切换的开发者操作区，支持单元解锁、满星和重置。
- 新增全局进度操作，支持全部单元一键解锁、全部满星和整体重置。
- 新增成就与错题管理功能，支持按单元清理错题和全量清理。
- 新增统计数据重置与默认存档恢复功能。
- 将旧的开发者工具页重构为分区式控制台，降低操作混乱度。
- 移除对固定 6 关结构的依赖，改为按单元实际关卡数动态处理。
- 优化开发者操作反馈，状态区现在会显示时间戳。
- 优化开发者界面样式与响应式布局，兼容桌面端和移动端。
- 重新生成发布文件 `dist/Python基础闯关_正式版.html`，确保开发版与正式版一致。
