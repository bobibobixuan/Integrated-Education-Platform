# 2.1.29

- 修复学生端把判断题误显示成填空题的问题，现在会直接显示“正确 / 错误”两项选择。
- 后端判断题判题兼容旧题库答案格式，支持 `A/B`、`正确/错误`、`对/错`、`True/False` 等多种等价写法。
- 判断题反馈中的正确答案展示已同步优化，不再只显示原始字母。
- 已验证：`frontend` 下 `npm run build` 成功，判断题判题兼容逻辑已完成代码级验证。
