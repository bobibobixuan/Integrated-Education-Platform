# 2.1.27

- 修复学生端把历史多选题显示成单选题的问题，冒险、练习、极限三种作答页现在都支持多选。
- 后端增加题型兜底逻辑：历史错标数据即使还没手工修，也会按“多选题”返回给前端。
- 当前数据库里的历史错标题目已批量修正，原先 `50` 道答案为多项字母组合但题型写成单选的题目已统一改为 `多选题`。
- 已验证：`frontend` 下 `npm run build` 成功，后端主应用导入成功，数据库中的此类错标题目已清零。
