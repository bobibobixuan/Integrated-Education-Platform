# 2.1.8 — 教师端入口切换到新前端

- 停用后端对 `dev/admin` 旧静态教师端的直接挂载，避免老师继续误进旧页面。
- ` /admin ` 现在统一重定向到新的 Vue 管理端 ` /app/admin `，教师端只保留新框架一套入口。
- 根路径 ` / ` 改为进入学生端 ` /app/ `，与当前前后端拆分后的路由结构保持一致。
- 验证通过：`backend.app.main` 导入成功，`start.bat` 启动后可正常拉起 FastAPI 服务。
