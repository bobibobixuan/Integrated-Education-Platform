恢复消失的页面动画。
- App.vue 用 `<Transition mode="out-in">` 包裹 `<router-view>`，页面切换 fadeUp 过渡。
- AdminView 模态框加 scaleIn 入场动画。
- style.css 补 `spin`/`pulse` keyframe。
- 加 `prefers-reduced-motion` 媒体查询，尊重用户无障碍偏好。
