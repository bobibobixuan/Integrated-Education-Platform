# 2.1.12

- 修复教师端偶发“进入后台后没有数据”的会话恢复问题。
- 新教师端现在会兼容旧静态老师端留下的 `admin_jwt_token`，自动迁移到当前使用的 `admin_token`。
- 如果浏览器里只剩下 `admin_user` 而没有任何可用 token，系统会自动清理残留状态并回到登录页，不再出现“看起来已登录、实际拉不到数据”的空后台。
- 已验证两种关键场景：旧 token 可直接恢复仪表盘与题库数据；孤立 user 状态会正确回退到登录页。
