# 2.1.14

- 教师端 PVP 房间配置不再让老师手填学生 ID 和单元 ID，已改为直接勾选的多选面板。
- 参赛学生支持按昵称、用户名和 ID 筛选，并提供“全选当前结果 / 清空”快捷操作。
- 出题单元支持按单元名称、说明和 ID 筛选，保存时直接提交选中的单元集合。
- 已验证：`frontend` 下 `npm run build` 成功，并已登录教师端打开 PVP 新建房间弹窗检查学生与单元选择器。
