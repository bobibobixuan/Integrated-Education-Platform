- 修复竞技场房间在倒计时结束后没有自动进入对战的问题；后端现在会到点自动开战并把 `running` 状态推送给学生端。
- 学生端竞技场增加本地到点兜底，即使个别推送迟到，也会主动补请求进入对战。
- 额外检查了学生端固定 DOM 节点依赖，没有再发现和 `floatingCoins` 同类的“模板已删但脚本仍硬依赖”问题。
- 学生端静态资源版本升级到 `1.0.40`，确保浏览器拿到新的自动进战逻辑。
