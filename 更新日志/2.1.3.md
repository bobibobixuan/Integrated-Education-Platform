# 2.1.3 — 选择题判题修复

- 修复学生端选择题“点对了却判错”的问题：当题库答案存为 `A/B/C/D`、前端提交为选项文本时，后端现在会把字母答案和文本答案映射到同一选项后再判题。
- 普通闯关和 `PVP` 对战统一使用这套新判题逻辑。
- 学生端选择题提交流同步改为优先提交选项字母，减少前后端答案格式不一致造成的误判。
- 验证通过：后端回归测试 4/4 通过，前端 `npm run build` 成功。
