# 2.1.9 — 学生端布局优化

- 学生端首页改成更清晰的两段式结构：上半区负责欢迎信息、学习概览和账号操作，下半区再分成主要入口与辅助入口。
- 主模式卡片改为桌面端双列、移动端单列的响应式布局，不再把四个核心模式和辅助功能硬压在同一层级里。
- `AppShell` 顶栏、`page-shell`、`page-surface`、`surface-panel` 一并优化宽度、留白和小屏规则，学生端子页面在平板和手机上更容易阅读和操作。
