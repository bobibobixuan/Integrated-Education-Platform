# 2.2.2

- 版本号提升到 `2.2.2`，后端 API 版本和启动脚本显示已同步更新。
- 修复 `PVP` 自动开战后前端把 UTC 时间当成本地时间解析的问题，房间开始时间和结束时间现在统一输出带 `+00:00` 的 UTC 字符串。
- 前端倒计时兼容旧的无时区时间字符串，按 UTC 解析，避免 300 秒总时限被误判为已经结束。
- 后端结算接口新增保护：未到总时限且仍有未答题时拒绝提前结算请求，防止客户端倒计时误判直接扣分并结束房间。
- 已补充后端回归测试，覆盖时间序列化和提前结算保护。
