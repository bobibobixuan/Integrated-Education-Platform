- 学生端与管理端 UI 统一重做为专业化深色数据台风格，收口导航、标题区、卡片、表格、按钮、表单和状态标签视觉语言
- 重写 `LoginView.vue`、`GameView.vue`、`LeaderboardView.vue`、`AchievementsView.vue`、`PvPLobbyView.vue`、`PvPBattleView.vue` 与 `AdminView.vue`，各模块改为只保留功能信息与操作入口
- 更新 `src/components/AppShell.vue` 与 `src/style.css`，建立学生端/管理端共用的侧边导航、内容网格和统一控件体系
- 管理台统一接入学生管理、用户列表、题目管理、竞技房间四个模块的工具栏、数据卡、表格和弹层样式
- 调整前端构建入口，显式固定 Vite build root，恢复当前 Windows 环境下 `npm run build` 的稳定性
- 验证通过：`npm run build` 成功，`npm test` 3/3 通过，并已用本地浏览器预览复核关键页面
