- 学生端主逻辑按功能域拆分：核心玩法保留在 `20_app.js`，学习记录/错题分析拆到 `21_progress.js`，竞技场/排行榜拆到 `22_arena.js`，密码修改与战力结算拆到 `23_account.js`。
- `dev/index.html` 同步调整脚本加载顺序，避免拆分后全局函数初始化顺序混乱。
- 学生端静态资源版本升级到 `1.0.41`，确保浏览器拿到新的文件结构。
