- 竞技场对战题流从冒险模式中独立出来：学生端进入 `PVP` 后不再复用普通闯关的取题/交题接口。
- `PVP` 开战时只会下发当前题，不再预先把整包题目、正确答案和解析一次性发到前端。
- 对战答题现在由后端严格控制顺序；乱序提交、继续走冒险答题接口、以及房间结束后继续答题，都会被明确拒绝。
- 教师端不能再编辑进行中或倒计时中的 `PVP` 房间，避免比赛中途把成员实时战力和准备状态重置掉。
- 学生端静态资源版本升级到 `1.0.42`，确保浏览器获取到新的竞技场独立答题链路。
