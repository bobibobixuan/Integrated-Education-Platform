# 2.1.10 — 学生端首页缓存修复

- 修复浏览器偶尔仍显示旧版学生首页的问题：`/app` 的 SPA 入口页现在显式返回 `Cache-Control: no-store`。
- 只禁止入口 HTML 缓存，哈希化的 JS/CSS 静态资源仍然保持正常缓存，不影响加载性能。
- 验证通过：后端测试请求已确认 `/app/` 响应头为 `Cache-Control: no-store, max-age=0`。
