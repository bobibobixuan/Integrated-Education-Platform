# 2.1.31

- 修复 `PVP` 重新开新一轮时误复用旧会话的问题：现在只会读取当前这一轮 `battle_started_at` 对应的会话，不会再把上一轮已完成的答题状态带进新一轮。
- 学生端房间准备页新增可见倒计时提示，全员准备后会显示开战倒计时和自动进入对战的说明。
- 倒计时结束后会自动从房间准备页跳转到对战作答页，不再要求学生手动点击“进入对战”。
- `PVP` 房间状态广播已补全：准备、取消准备、退出房间、教师开始和结束对战后，成员页会更快拿到最新状态。
