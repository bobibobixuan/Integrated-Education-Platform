# 2.2.23 — 成就墙接入真实数据

- 后端新增默认成就补种与自动解锁评估，成就墙现在会显示真实成就列表，不再只有空状态。
- `GET /api/achievements/` 返回补充 `unlocked_at`，学生端可直接展示解锁状态和最近解锁成就。
- `POST /api/records/answer` 现在会返回本次新解锁的成就摘要，便于后续在答题结果页做即时提示。
- 修正选择题答案兼容性：答案存文本、提交选项字母时也能判对。
