# 2.2.3

- 版本号提升到 `2.2.3`，后端 API 版本和启动脚本显示已同步更新。
- 修复学生端实时对战分数不同步的问题，登录恢复后现在会自动建立 `WebSocket` 连接，答题后的房间状态广播可以及时推送到其他在线成员。
- 修正学生端 `WebSocket` 心跳和认证状态处理，前端现已发送服务端识别的 `heartbeat`，并按 `authenticated` 状态维持连接可用性，减少必须手动刷新后才看到别人分数变化的情况。
