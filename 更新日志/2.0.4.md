- 新增 `src/components/AppShell.vue` 统一应用外壳：顶部导航、用户区、标题英雄区、统一内容容器
- 重写 `src/style.css` 全局主题变量与基础控件样式，建立暖色冒险视觉体系
- 重写 `LoginView.vue`：双栏登录首页、任务板、功能标签、渐变主按钮
- 重写 `GameView.vue`：地图式单元卡、选关侧栏、答题舞台、结果结算卡片
- 重写 `LeaderboardView.vue`：榜单切换、前三名 podium、我的排名横条、统一表格外壳
- 重写 `AchievementsView.vue`：成就墙卡片、解锁态/未解锁态、稀有度标签
- 重写 `PvPLobbyView.vue` 与 `PvPBattleView.vue`：房间准备区、即时排名、反馈浮层、结算态
- 新增 `src/api/http.ts`，统一处理空响应/非 JSON 响应，避免前端界面直接显示底层解析异常
- 更新 `auth.ts` / `game.ts` / `leaderboard.ts` / `achievements.ts` / `pvp.ts` / `admin.ts` 改用统一请求封装
- 修复管理员页刷新后丢失入口的问题：`auth` store 现持久化用户快照，并在应用启动时自动恢复 `/api/auth/me` 登录态
- 更新 `main.ts` 启动流程，挂载 Pinia 后立即尝试恢复会话
- 更新 `AppShell` 兼容无 router 注入的组件测试环境
- 验证通过：`npm run build` 成功，`npm test` 3/3 通过，Playwright 已截图复测登录页与游戏页壳层
