代码审查修复汇总（11 项中的 10 项）。

**后端**：
- PVP 选题：删除 `pvp.py` 内联固定顺序选题，统一走 `ensure_pvp_battle_session()`（随机选题 + 旧会话废弃）。
- 成员序列化：删除 `pvp.py:_serialize_members`，复用 `serialize_member` + `rank_members`（补 is_online/accuracy/best_streak/last_answer_at）。
- 提取 `backend/app/utils.py`：`utcnow()` / `as_utc()` 共享工具，`pvp.py` 和 `records.py` 统一引用。

**前端**：
- 类型修复：`PvpBattleSession.room` 从 `Record<string, unknown>` 改为 `PvpRoom`，`PvpMember` 补全字段。
- 排名竞态防护：WS handler 用 `started_at` 时间戳比较，拒绝旧消息覆盖新状态。
- 对战倒计时：`startBattleTimer` 从 `started_at` + `battle_time_limit_seconds` 计算剩余秒数，状态栏显示（最后 60 秒高亮）。
- 开发者文案：PvPBattleView 4 处改为面向学生的产品文案。
- 倒计时器：`setInterval` 改为递归 `setTimeout`，避免卸载时多跑一次。
- `handlePvpCheckboxChange`：`event.target` 改为 `event.currentTarget`。
